# 课程写法规范（Teaching Contract）

> 这份文档规定**每一章怎么讲**。它存在的理由是一个真实的读者反馈：
>
> > "正常人写的时候肯定是有一个想法，然后想实现，用什么库什么函数可以实现这个功能。
> > 你省略掉了前面这么多东西……默认掌握前置知识。"
> >
> > "读者看到 MiniAgent，但不知道 TOOLS、Message、FINAL_RE 从哪来，根本跑不起来。"
>
> 所以规范的目标只有一个：**让读者从零推导出代码，而不是从代码倒着猜。**

---

## 0. 最高原则

```
先讲"要做什么" → 再讲"现在卡在哪" → 再讲"哪个库/哪个函数能解决"
→ 立刻用一次 → 再遇到下一个卡点 → 再找工具 → ……
```

**代码是结论，不是起点。**

### 0.1 两条不冲突的规矩（★ 最容易写错的地方）

| 规矩 | 说的是 | 放在哪 |
|---|---|---|
| **逐步给** | 讲解的**时机**：每个知识点在"读者正好需要它"的那一刻出现 | 正文里 |
| **前置知识表** | 查阅的**索引**：本章用到的所有符号汇总成表 | 开头一节（可跳过） |

**两者都要有，但职责不同：**

```
【开头】⓪ 本章速查表        ← 一张表，供"读到一半忘了"时回头查
                             明确标注：初次阅读可以跳过

【正文】① 遇到问题 A
           → 需要什么能力
           → 这个库/函数能解决（就地讲清：是什么/参数/返回/例子）
           → 立刻用它（一个自包含的代码单元）
        ② 遇到问题 B
           → 同上
        ……

【收口】要点 / 坑表 / 练习 / 自检
```

### 0.2 正文里禁止的写法

❌ **禁止**：在正文开头集中讲解本章会用到的全部符号，然后才开始正题。

```markdown
## 预备知识
| re   | 正则 | 抠结构 | re.compile(...) → 对象 |
| ast  | 语法树 | 安全求值 | ast.parse(...) → 树 |
| json | JSON | 转换    | json.loads(...) → dict |
（一张 15 行大表）

## ① 开始写代码
...出现了 re.compile(...)
```

**为什么不行**：读者读到代码时，前面那张表已经忘了，得翻回去查；
而且他当时并不知道"我为什么需要正则"——表格只回答了"正则是什么"。

✅ **正确**：把同一张表移到开头的「速查表」里当索引用，正文改成：

```markdown
## ① 让模型算个数

...（先说清：模型算长表达式会错，所以要让它申请调用计算器，我们的代码去算）

## ② 从模型的输出里取出"它想调什么"

模型输出的是我们约定的格式：<call>{"name": "calc", "args": {...}}</call>
现在的问题：**怎么从一段文本里把 {"name": ...} 挖出来？**

这需要"正则表达式"。Python 里用 re 模块：
    re.compile(正则字符串, 标志) → 一个 Pattern 对象
    Pattern.search(文本)         → Match 对象，找不到返回 None
（就地讲清 + 一行可跑的例子）
```

区别在于：读者**先有了"我需要一个能从文本里挖东西的工具"这个念头**，
然后才看到正则 —— 这时候信息才接得上。

### 0.3 每个知识点的固定节奏

正文里每引入一个新东西，按这个顺序，**中间不要插入别的内容**：

```
1. 我们现在卡在哪      —— 用一句话说清当前的问题
2. 所以我需要一个能……的东西
3. 它就是 XXX          —— 哪个库/哪个函数
4. 它的用法            —— 签名 / 参数 / 返回什么 / 一行可跑的例子
5. 立刻用一次           —— 一个自包含的代码单元，跑出结果
6. 看看结果说明什么      —— 解释输出，然后进入下一个卡点
```

### 0.4 三个必须回答的问题

任何一段代码出现之前，读者心里应该已经有答案：

| 问题 | 例子 |
|---|---|
| 我为什么要这个东西？ | "模型没有记忆，所以我要自己维护一个列表" |
| 为什么用它而不是别的？ | "用 `ast` 而不是 `eval`，因为输入不可信" |
| 它给我什么？怎么用？ | "`re.compile()` 返回 Pattern，`.search()` 返回 Match 或 None" |

---

## 0.5 读者画像与三个必须回答的问题

面向**编程能力已具备、但工程经验刚起步**的读者（算法竞赛背景）。
所以：不解释循环/字典/类是什么；但**必须**解释每个库、每个函数、
每个数据结构的字段 —— 因为这些不是"通用编程常识"，
而是本课程/本项目特有的东西。

---

## 1. 第 00 章必须先讲"模型和代码之间传什么"

这是**地基**。读者如果不知道模型和程序之间靠什么通信，后面所有代码都是悬空的。

必须在第 00 章讲清的事实（用一个完整的小例子演示，不要只写结论）：

```
        你的程序                                模型
           │                                     │
           │  ① 发过去：一段文本                    │
           │     · 系统提示词（我是谁、有哪些工具、输出格式）
           │     · 对话历史（用户说了什么、工具返回了什么）
           ├────────────────────────────────────>│
           │                                     │
           │                          ② 它继续往下写一段文本
           │                                     │
           │  ③ 收回来：一段文本                    │
           │<────────────────────────────────────┤
           │                                     │
           │  ④ 我们用正则解析这段文本：             │
           │     它是要调工具，还是在回答？           │
           │                                     │
           │  ⑤ 如果要调工具：我们自己执行，          │
           │     把结果当成"新的一条消息"再发回去      │
           └─────────────────────────────────────┘
```

**关键认知（很多教程跳过这一步）：**

1. **模型和程序之间只传文本。** 没有"函数调用"这种魔法 ——
   所谓 function calling 也是"模型输出一段特定格式的文本，
   厂商的 SDK 帮你解析成结构体"而已。
2. **模型不能执行任何东西。** 工具是**你的程序**执行的。
   模型只负责"说它想调什么"。
3. **模型没有记忆。** 每次都要把完整历史重新发过去。
4. **所以"让模型用工具"这件事，本质是：约定一套文本格式 + 写一个解析器。**

第 00 章要带读者**亲眼看一遍**这四件事，用最小的例子（几十行、可运行）。

---

## 2. 每章的标准骨架

```
① 这一章要解决什么问题
      · 上一章留下了什么坑？（用可观察的现象说明，不要抽象描述）
      · 如果不管它，会发生什么坏结果？

② 拆解：我需要哪些能力
      · 把问题拆成 2~5 个"我需要一个能做 X 的东西"
      · 尽量让读者自己得出结论："所以我需要一个 XXX"

③ 有哪些现成的东西可以用
      · 对每个能力，给出候选：
          - 标准库里的哪个模块/函数？签名是什么？返回什么？
          - 本项目的 core/ 里有没有现成的？从哪 import？
          - 没有现成的 → 那就自己写，但要说清"为什么没有现成的"
      · 每个都给出：它是什么 / 参数要什么 / 返回什么 / 一行可跑的例子

④ 数据结构：东西存哪
      · 中间结果用什么结构装？list / dict / dataclass？
      · 为什么是这个结构？（不是随便挑的，要说理由）
      · 如果用了 dataclass，逐个字段说明含义

⑤ 联动：它们怎么串起来
      · 画出数据流：谁把什么传给谁，顺序是什么
      · 说清"哪一步会出错、出错时怎么办"

⑥ 完整代码 + 跑一次
      · 给出可直接运行的完整代码
      · 打印真实的每一步中间状态（让读者看到数据在流动）
      · 给出预期输出

⑦ 收口
      · 一句话本质
      · 常见坑表
      · 练习（含至少一个"拆掉护栏看后果"）
      · 自检
```

**不是每一节都要长篇大论。** 第 ③ 节如果只有一个显然的选择，三行带过即可；
但如果读者可能不认识那个库，就必须讲清。

---

## 3. 代码单元的硬规矩（机器校验）

由 `notebooks/nb_lint.py` 强制检查，违反会被测试卡住：

### 规矩 1：每个代码单元都能单独运行

把任意一格单独复制出去，必须能跑通、不报 `NameError`。

**实现方式**：每一格需要的 `import`、常量、函数定义都写在**这一格之内**。
允许重复（每个单元重 import 一次）。

> 为什么宁可重复？因为读者的实际行为是"看到某一段想试试，就复制走"。
> 一旦复制出来报 `NameError`，他就卡住了 —— 而你不在旁边。

**例外**：如果某格**刻意**接着上一格的变量继续（比如"还是用刚才那个 `result`"），
必须在 `nb_lint` 的 `CELL_DEPS` 里显式声明，并渲染成一个"⚠️ 本格依赖第 N 格"的提示单元。

### 规矩 2：每个符号首次出现要交代三件事

```python
# re.compile(正则字符串, 标志) → 返回 Pattern 对象
#   参数1 pattern：正则表达式，注意 . * ? ( ) 是**语法**不是普通字符
#   参数2 flags  ：最常用的是 re.S，让 . 也能匹配换行符
#   返回        ：Pattern 对象，用它的 .search(文本) 方法
TOOL_CALL_RE = re.compile(r"<tool_call>(.*?)</tool_call>", re.S)
```

**不允许出现"读者不认识但没说"的名字**，无论它是：
- Python 内置（`compile` / `len` / `isinstance`）
- 标准库（`re.compile` / `json.loads` / `ast.parse`）
- 本项目的（`Message` / `LLMResponse` / `build_default_registry`）

### 规矩 3：不使用读者尚未见过的概念

每章开头有一节"**本章前置**"，列出：
- 需要上一章的什么结论（并给出回看链接）
- 需要哪些 Python/标准库知识（如果超出基础范围，就地补）

---

## 4. 讲解"库函数"的固定格式

凡是介绍一个新函数，按这五味写：

```
1. 它是谁         —— 哪个模块的（标准库？本项目？）
2. 为什么需要它    —— 对应第 ② 节的哪个能力
3. 签名           —— 参数名、类型、哪个必填哪个可选
4. 返回什么        —— 类型 + 里面的字段（★ 读者最需要这个）
5. 一行可跑的例子  —— 含真实输出
```

例子（讲 `json.loads`）：

```
1. 它是谁        : 标准库 json 模块的函数
2. 为什么需要它   : 模型给的参数是**文本**，我们要变成能传给函数的**字典**
3. 签名          : json.loads(s)   ← s 是字符串
4. 返回什么       : dict 或 list（取决于 JSON 内容）；不是合法 JSON 会抛 JSONDecodeError
5. 例子          : json.loads('{"expr": "1+1"}')  →  {'expr': '1+1'}
```

---

## 5. 画数据流的要求

"它们怎么联动"这一节**必须有一张图**，让读者看到数据在流动。用 ASCII 即可。

例子（第 01 章的循环）：

```
    ┌──────────────────────────────────────────────┐
    │  messages: list[Message]        ← Agent 的记忆 │
    │  [system] [user] [assistant] [tool] ...       │
    └───────────────────┬──────────────────────────┘
                        │ 整个列表发过去
                        ▼
                 llm.complete(messages)
                        │
                        ▼  LLMResponse
                  .text = "Thought: ...\n<tool_call>{...}</tool_call>"
                        │
                        ▼  用正则解析
              ┌─────────┴─────────┐
              ▼                   ▼
        解析出工具调用        解析出 Final Answer
              │                   │
              ▼                   ▼
        执行工具函数           结束，返回答案
              │
              ▼ 把结果包装成 Message
        messages.append(...)  ──► 回到开头，再来一圈
```

---

## 6. 术语与命名的一致性

| 概念 | 统一用词 | 不要混用 |
|---|---|---|
| 模型返回的文本 | **模型输出** | 回复 / 回答 / response |
| 需要模型调用的函数 | **工具** | function / API / 接口 |
| 对话历史列表 | **历史** / `messages` | 上下文 / memory（除非特指记忆章节） |
| 循环的一轮 | **圈** | 步 / 轮 / iteration |
| 模型自己写的推理 | **Thought** | 思考链 / CoT |

---

## 7. 每章的验收（自动检查）

一章算写完，必须同时满足：

```powershell
py scripts\build_notebooks.py NN        # 所有代码单元真的跑通
py scripts\check_structure.py NN        # 材料齐全、契约方法在
py scripts\run_all_checks.py NN         # 教学验收标准通过
python -c "from notebooks.nb_lint import lint_notebook; ..."   # 自包含检查 0 违规
python -m unittest discover -s tests -t .   # 含"每格独立子进程跑"的硬测试
```

---

## 8. 改造进度

**全部 14 个 Notebook 已按本规范重写完毕**（2026-09-16）。
`notebooks/nb_lint.py` 机器校验：**未声明依赖合计 0**。

| 章 | 生成器源码 | 自包含 | 按新骨架重写 |
|---|---|:---:|:---:|
| 00 模型和代码传什么 | `nb0_foundation.py` | ☑ | ☑ |
| 01 最小 Agent 循环 | `nb1_ch01.py` | ☑ | ☑ |
| 02 工具系统 | `nb2_ch02.py` | ☑ | ☑ |
| 03 ReAct 提示工程 | `nb3_ch03.py` | ☑ | ☑ |
| 04 规划 | `nb3_ch04.py` | ☑ | ☑ |
| 05 记忆 | `nb4_ch05.py` | ☑ | ☑ |
| 06 RAG | `nb5_ch06.py` | ☑ | ☑ |
| 07 反思 | `nb6_ch07.py` | ☑ | ☑ |
| 08 多智能体 | `nb7_ch08.py` | ☑ | ☑ |
| 09 工作流状态机 | `nb8_ch09.py` | ☑ | ☑ |
| 10 评估可观测 | `nb9_ch10.py` | ☑ | ☑ |
| 11 安全护栏 | `nb10_ch11.py` | ☑ | ☑ |
| 12 成本延迟 | `nb11_ch12.py` | ☑ | ☑ |
| 13 生产化部署 | `nb12_ch13.py` | ☑ | ☑ |

### 防退化机制

重写过程中建立的三道自动检查，会持续守住这套标准：

| 机制 | 文件 | 作用 |
|---|---|---|
| **静态自包含检查** | `notebooks/nb_lint.py` | 用 CPython 的 `symtable` 分析符号表，找出"引用了但本单元没定义"的名字 |
| **动态独立执行测试** | `tests/test_core.py::TestCellSelfContained` | 把**每个代码单元**丢进独立子进程真跑一遍，退出码必须为 0（最硬的证据） |
| **产物新鲜度检查** | `scripts/check_structure.py` | 生成源码比 `.ipynb` 新 → 报红，提示重新生成（防编辑器旧内容覆盖） |

一条命令恢复被覆盖的产物：`py scripts\rebuild_all.py`
