# 学习笔记 · Agent 开发

> 这不是课程内容，而是**你自己的笔记本**。
> 用法：每学完一章，在下面追加一条记录。写不出来的地方，就是你还没懂的地方。
>
> **一条笔记合格的标准**：能用**自己的话**复述「一句话本质」，并说出一个自己踩过的坑。

---

## 0. 复盘模板（复制这一段用）

```markdown
### 第 NN 章 · <标题>        日期：____

**一句话本质（自己复述，不许抄）**
>

**这一章解决了上一章的什么问题？**
>

**我自己踩到的坑 / 观察到的反常现象**
>

**改动实验**：我改了什么 → 结果如何 → 为什么
>

**还没搞懂的地方（下次回来解决）**
>

**自检结果**：`py scripts\run_all_checks.py NN` → __ 项通过
```

---

## 1. 课程进度表

| 章 | 主题 | 读完 | 跑通 demo | 做完练习 | 自检通过 |
|---|---|:---:|:---:|:---:|:---:|
| 01 | 最小 Agent 循环 | ☐ | ☐ | ☐ | ☐ |
| 02 | 工具系统与 JSON Schema | ☐ | ☐ | ☐ | ☐ |
| 03 | ReAct 提示工程 | ☐ | ☐ | ☐ | ☐ |
| 04 | 规划与任务分解 | ☐ | ☐ | ☐ | ☐ |
| 05 | 记忆与上下文工程 | ☐ | ☐ | ☐ | ☐ |
| 06 | RAG 检索增强 | ☐ | ☐ | ☐ | ☐ |
| 07 | 反思与自我修正 | ☐ | ☐ | ☐ | ☐ |
| 08 | 多智能体协作 | ☐ | ☐ | ☐ | ☐ |
| 09 | 工作流与状态机 | ☐ | ☐ | ☐ | ☐ |
| 10 | 评估与可观测性 | ☐ | ☐ | ☐ | ☐ |
| 11 | 安全护栏 | ☐ | ☐ | ☐ | ☐ |
| 12 | 成本与延迟优化 | ☐ | ☐ | ☐ | ☐ |
| 13 | 生产化部署 | ☐ | ☐ | ☐ | ☐ |

---

## 2. 核心结论速查（学完回来核对）

这些是本课程的"骨架结论"。**先自己写一遍，再和这里对照** ——
如果你写的和这里差得远，说明那一章还没吃透。

<details>
<summary>点开看参考答案（建议先自己写）</summary>

### 第 01 章
**本质**：Agent = 一个 while 循环 + 一个会调工具的模型 + 一个能记住历史的列表。
**关键认知**：Agent 没有记忆，它只是每次把整段历史重新发给模型。
**必答**：`max_steps` 不是可选项 —— 它是唯一能兜住"模型犯傻"的机制。

### 第 02 章
**本质**：工具 = 函数签名（给模型看）+ 参数校验（保护自己）+ 结果序列化（给模型读）。
**关键认知**：模型输出是不可信输入；校验必须在**执行之前**。
**必答**：为什么不能用 `eval` —— 因为参数来自模型输出，而模型输出可被用户间接控制。

### 第 03 章
**本质**：提示词 = 身份规则 + 工具说明 + **输出格式契约** + 参考资料。
**关键认知**：格式契约的读者是**解析器**，不是用户。
**必答**：`Agent 可靠性 = 模型指令遵循能力 × 格式契约清晰度 × 解析器健壮性`；
后两者你完全可控，所以先做到极致。

### 第 04 章
**本质**：规划 = 先产出步骤列表，再逐步执行 + 按需重规划。
**关键认知**：计划是**假设**，执行是**验证**，偏差就要修计划。
**必答**：为什么计划不能一次做完 —— 因为环境会变、假设会错。

### 第 05 章
**本质**：记忆 = 短期（消息窗）+ 工作（任务状态）+ 长期（外部存储）。
**关键认知**：上下文管理的本质是**在有限预算下保留最高价值的信息**。
**必答**：为什么简单截断最老消息是危险的 —— 关键事实（用户姓名、约束条件）可能就在里面。

### 第 06 章
**本质**：RAG = 切块 → 建索引 → 检索 → 注入上下文 → 带引用作答。
**关键认知**：**检索质量决定上限，生成质量只是下限**。
**必答**：检索不到时必须明确说"资料里没有"，而不是编造。

### 第 07 章
**本质**：反思 = 用另一个视角检查自己的输出 → 产出**具体可执行**的修正。
**关键认知**：没有"具体修正"的反思只是废话。
**必答**：为什么要有最大反思轮数 —— 否则模型会无限自我怀疑。

### 第 08 章
**本质**：多智能体 = 角色分工 + 消息传递 + 结果仲裁。
**关键认知**：收益来自专业化与并行，代价是**通信开销与不可控性**。
**必答**：多智能体不一定比单 Agent 强；先试单 Agent。

### 第 09 章
**本质**：能用确定性代码解决的，绝不用模型。
**关键认知**：模型只负责"需要判断"的节点，流程骨架应该是显式的图/状态机。
**必答**：`给 Agent 多少自由度` 的取舍，是成本 / 延迟 / 质量 / 可控性之间的权衡，不是"越自主越先进"。

### 第 10 章
**本质**：评估 = 数据集 + 指标 + 可复现的运行 + 回归对比。
**关键认知**：**没有评估集的优化都是玄学。**
**必答**：只知道总分下降没用，要能定位"是哪条用例退化了"。

### 第 11 章
**本质**：模型输出永远是不可信输入。
**关键认知**：护栏 = 输入过滤 + 权限最小化 + 高危审批 + 输出净化 + 审计日志。
**必答**：间接注入（恶意指令藏在工具返回里）比直接注入更危险。

### 第 12 章
**本质**：优化顺序 = 先测量 → 再缓存 → 再减量 → 最后才换模型。
**关键认知**：大部分成本来自"本可以不发生的模型调用"（行业经验值约八成，**非本课实测**）。
**必答**：别一上来就换便宜模型 —— 先看看有多少调用是重复的。

### 第 13 章
**本质**：生产化 = 状态持久化 + 幂等重试 + 并发控制 + 超时熔断 + 灰度回滚。
**关键认知**：核心矛盾是 **Agent 是有状态的长任务，而 HTTP 是无状态短连接**。
**必答**：本地跑通 ≠ 线上能跑，缺的是持久化/幂等/超时/评估。

</details>

---

## 3. 我的笔记

<!-- 从这里开始往下追加。最新的放最上面，方便回顾。 -->

### 第 01 章 · 最小 Agent 循环        日期：____

**一句话本质（自己复述，不许抄）**
>

**这一章解决了上一章的什么问题？**
> （第 01 章是起点，所以这里写"它要解决什么现实问题"）

**我自己踩到的坑 / 观察到的反常现象**
>

**改动实验**：我改了什么 → 结果如何 → 为什么
>

**还没搞懂的地方（下次回来解决）**
>

**自检结果**：`py scripts\run_all_checks.py 01` → __ 项通过

---

## 4. 坑位收集（边学边记，这是最值钱的部分）

> 别人的经验是知识，自己的坑才是能力。每踩一个坑就往这里记一条。

| # | 章节 | 现象 / 报错 | 根本原因 | 解法 |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

---

## 5. 我的实验记录

> 课程练习之外，你自己想试的东西记这里。包括失败的实验 —— 失败的更值得记。

| 日期 | 想验证什么 | 怎么做的 | 结果 | 结论 |
|---|---|---|---|---|
| | | | | |
| | | | | |

---

## 6. 待解决问题清单（跨章节的疑问）

> 学到某一章时冒出的、但当前章节不解决的问题，先记下来，学到对应章节再回来划掉。

- [ ] 例：多智能体的上下文隔离，在真实并发场景下怎么保证一致性？（第 13 章）
- [ ] 例：RAG 的切片大小怎么选？有没有可量化的方法？（第 10 章 + 第 06 章）
- [ ]
