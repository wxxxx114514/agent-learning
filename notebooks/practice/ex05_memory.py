r"""第 05 章 · 记忆与上下文工程 —— 练习工作区

这不是课程材料，是你的草稿纸：随便改、随便删，不会被任何检查覆盖，也不会被重新生成。

怎么用
    1. 在下面的「我的代码」区域写答案
    2. 运行这个文件看结果：  py notebooks\practice\ex05_memory.py
    3. 验收本章：            py scripts\run_all_checks.py 05
    4. 查 API（忘了某名字是什么）：  py -m notebooks.nb_explain explain <名字>

题目与提示来自（内容一致，不用两边翻）：
    notebooks/05_memory.ipynb 的「🏋️ 练习」节
    stages/stage05_memory/README.md 的「## 5. 练习」节
"""

import sys
from pathlib import Path

# 让下面的代码能 import 到课程框架（core / stages / notebooks）
sys.path.insert(0, str(Path(__file__).resolve().parents[2]))

from core.console import setup_console  # noqa: E402

setup_console()   # Windows 控制台默认 GBK；这一步让中文和 emoji 正常显示



# ==========================================================================
# 练习 1
# ==========================================================================
# **给钉住区加优先级抢占。**
#
# 现在 `MAX_PINNED=8`，超出的事实按重要性排序后**丢弃**。请改成「新事实抢占最低优先级的旧事实」，并打印每一次抢占（谁挤掉了谁）。
#
# ---- 提示（卡住了再看）-------------------------------------------
# 改动点在 `build()` 里的 `pinned` 切片处：先按重要性排序，插入新事实时把最弱的那条挤出去。
#
# 想清楚：被挤掉的事实要不要写进长期存储？（答案：要 —— 钉住区是「当前上下文的名额」，不是「存储」。）
# --------------------------------------------------------------------------
# 【我的代码】在下面写 👇



# 【我的代码】写完 👆


# ==========================================================================
# 练习 2
# ==========================================================================
# **给摘要加一个「覆盖度自检」。**
#
# 写一个 `summary_covers(summary, facts)`：检查所有钉住的事实是否都能在摘要里找到，并统计漏检率。目标是**量化摘要的漏检**，而不是假设它不漏。
#
# ---- 提示（卡住了再看）-------------------------------------------
# 用字符二元组做重叠度判断即可（复用 ⑤ 节的 `bigrams()`）。
#
# 连续跑 50 轮，统计摘要漏掉了哪几条 —— 你会发现它一直都在漏，只是漏的那些恰好被钉住区兜住了。
# --------------------------------------------------------------------------
# 【我的代码】在下面写 👇



# 【我的代码】写完 👆


# ==========================================================================
# 练习 3
# ==========================================================================
# **破坏护栏并观察：把钉住变成「什么都钉住」。**
#
# 把 `MAX_PINNED` 改成 `1000`，把阈值改成 `0.0`，然后跑 ③ 和 ⑦ 两格。
#
# 观察两件事：钉住区的 token 涨到多少？对话窗口被挤到只剩几轮？
#
# ---- 提示（卡住了再看）-------------------------------------------
# 你会看到对话窗口被挤到 1~2 轮 —— 这就是「永不淘汰的机制必须自带配额」的实证。
#
# 再想一步：如果这时用户问的是「我刚才说的最后一句话是什么」，Agent 还能答对吗？（窗口里还有东西吗？）
# --------------------------------------------------------------------------
# 【我的代码】在下面写 👇



# 【我的代码】写完 👆


# ==========================================================================
# 练习 4
# ==========================================================================
# **换一种淘汰策略：按「重要性密度」淘汰。**
#
# 现在超窗的消息是**整轮**折进摘要。请改成按「每条消息的价值 / token 数」排序淘汰，优先保留高密度的消息。
#
# ---- 提示（卡住了再看）-------------------------------------------
# 价值可以先用「这条消息里抽出的事实数」近似。
#
# 对比一下：你的新策略和「整轮折叠」相比，同样的预算下多保住了几条事实？这就是第 10 章「用评测集做优化」的雏形 —— 先有数字，再谈改进。
# --------------------------------------------------------------------------
# 【我的代码】在下面写 👇



# 【我的代码】写完 👆


# ==========================================================================
# 练习 5
# ==========================================================================
# **把规则摘要换成模型摘要。**
#
# 写一个假模型（继承 `core.llm.LLM`，实现 `_complete()`），让它扮演摘要模型，用 `SUMMARY_PROMPT` 那种提示词。对比两种摘要的 token 数与关键事实保留率。
#
# 然后回答一个工程问题：**摘要调用失败时（返回空 / 超长 / 胡说）怎么办？**
#
# ---- 提示（卡住了再看）-------------------------------------------
# 假模型可以用固定字符串拼接，只要保证确定性即可。
#
# 失败处理至少要有三条：空摘要不能覆盖旧摘要、超长要截断、失败时保留旧摘要并记一条告警 —— **绝不因为摘要失败而丢历史**。
# --------------------------------------------------------------------------
# 【我的代码】在下面写 👇



# 【我的代码】写完 👆


if __name__ == "__main__":
    print("第 05 章练习工作区 —— 把你的代码写到上面的区域，然后重跑本文件。")
    print("验收本章： py scripts\\run_all_checks.py 05")
